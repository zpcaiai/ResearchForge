"""ResearchForge Python core.

The Python side owns everything that touches papers, literature, code and data.
The TypeScript side owns orchestration, gating and the human interface. The
boundary between them is generated from `manifests/artifact-graph.json`.
"""
__version__ = "0.3.0"
